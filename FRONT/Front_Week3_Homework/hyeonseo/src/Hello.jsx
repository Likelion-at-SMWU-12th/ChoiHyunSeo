import React from "react";

const Hello = () => {
  const name = "고양이지대조아";
  if (name == "고양이지대조아") return <>조지냥! 야옹이의 페이지입니다!</>;
  return <>고양이를 좋아하지 않는다면 출입할 수 없습니다.</>;
};

export default Hello;
